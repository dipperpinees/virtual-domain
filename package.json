{
    "name": "@bsscommerce/tunnel",
    "version": "1.0.1",
    "main": "dist/index.js",
    "license": "MIT",
    "scripts": {
        "test": "jest",
        "postinstall": "node ./scripts/postinstall.js",
        "build": "rimraf ./dist && tsc"
    },
    "files": [
        "scripts",
        "src"
    ],
    "dependencies": {
        "isomorphic-fetch": "^3.0.0",
        "ngrok": "^4.3.3",
        "prompt-sync": "^4.2.0"
    },
    "devDependencies": {
        "@types/prompt-sync": "^4.2.0",
        "rimraf": "^5.0.1",
        "typescript": "^5.1.6"
    }
}
